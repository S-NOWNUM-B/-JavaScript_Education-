//const balance = 900;
//const bonusBalance = 10;
//const isBanned = false;
//const isExist = true;
//const isSelling = true;

//console.log(isSelling && isExist && !isBanned && (balance >= 1000 || bonusBalance >= 100) ? 'Доступ разрешен' : 'Доступ запрещен');